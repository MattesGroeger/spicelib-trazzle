# Properties file for build.xml
library.name = air-icon-badge

# Version number for current releases
version = 0.1
library.name.versioned=${library.name}-${version}

# Flex SDK locations
compc = ${flex3sdk.dir}/bin/compc${file.extension.exe}
flexsdk.lib.dir = ${flex3sdk.dir}/frameworks/libs
playerglobal.swc = ${flexsdk.lib.dir}/player/10/playerglobal.swc
flex.tasks = ${flex3sdk.dir}/ant/lib/flexTasks.jar

# Build locations
src.dir = src
libs.dir = libs
deploy.dir = bin